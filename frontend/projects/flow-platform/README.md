# flow-platform

Versioniertes Angular-Basispaket für Flow-Verträge, HTTP-Zugriff, Engine, Berechtigungen und den erweiterbaren Renderer.

```bash
npm run build:flow-platform
ng test flow-platform --watch=false
```
